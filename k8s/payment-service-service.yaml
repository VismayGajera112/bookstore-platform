apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: bookstore
  labels:
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8084
      targetPort: http
