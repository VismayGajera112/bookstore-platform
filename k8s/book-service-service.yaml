apiVersion: v1
kind: Service
metadata:
  name: book-service
  namespace: bookstore
  labels:
    app: book-service
spec:
  type: ClusterIP
  selector:
    app: book-service
  ports:
    - name: http
      port: 8082
      targetPort: http
